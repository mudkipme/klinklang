#!/usr/bin/env node

var debug = require('debug')('klinklang');
var app = require('../app');

app.set('port', process.env.PORT || 3393);

var server = app.listen(app.get('port'), function(){
  debug('Klinklang server listening on port ' + server.address().port);
});